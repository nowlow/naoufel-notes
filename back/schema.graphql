### This file was autogenerated by GraphQL Nexus
### Do not make changes to this file directly


"""Comment table"""
type Comment {
  content: String!
  date: DateTime!
  id: Int!
}

scalar DateTime

type Mutation {
  addComment(content: String, note_id: Int): Comment!
  addNote(content: String, title: String): Note!
}

"""Note table"""
type Note {
  comments: [Comment!]!
  content: String!
  date: DateTime!
  id: Int!
  title: String!
}

type Query {
  getComments(note_id: Int): [Comment!]!
  getNote(id: Int): Note!
  getNotes: [Note!]!
}
